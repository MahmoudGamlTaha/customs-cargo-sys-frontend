// Firebase configuration has been removed as per user request to eliminate database dependency.
// The application now runs on mock data.

const app = {};
const auth = {};
const db = {};

export { app, auth, db };
